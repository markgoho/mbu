{
  "name": "mbu",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "sync:badges": "bun scripts/sync-requirements-hybrid.ts",
    "detect:links": "bun scripts/detect-related-badges.ts",
    "build:local": "rm -rf hugo/public && cd hugo && hugo && bun run index",
    "build": "cd hugo && hugo --minify && bun run index",
    "hugo:dev": "cd hugo && hugo server",
    "index": "bunx pagefind --site hugo/public"
  },
  "devDependencies": {
    "@google/genai": "^1.34.0",
    "@mendable/firecrawl-js": "^4.9.3",
    "@types/bun": "latest",
    "firebase-tools": "^15.1.0",
    "js-yaml": "^4.1.1",
    "pagefind": "^1.4.0",
    "prettier": "^3.7.4",
    "prettier-plugin-go-template": "^0.0.15",
    "prettier-plugin-organize-imports": "^4.3.0",
    "sharp": "^0.34.5",
    "zod": "^4.2.1"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "cheerio": "^1.1.2",
    "impit": "^0.7.5",
    "slugify": "^1.6.6"
  }
}
